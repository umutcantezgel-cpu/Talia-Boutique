# Project Plan

## Milestones
1. Update `app/lookbook/page.tsx` to support scrolling through the new videos.
2. Update `components/ui/instagram-grid.tsx` to play videos (`autoplay loop muted playsinline`) instead of images. Also update `MOCK_INSTAGRAM` in `lib/mock-data.ts` to use videos.
3. Add `LocalBusiness` Schema.org JSON-LD to `app/page.tsx` for Wetzlar local SEO.
4. Add 3 new blog posts to `MOCK_JOURNAL` in `lib/mock-data.ts` targeting local SEO (Wetzlar, Hochzeitsmode) and assign the generated images to them.
